# 作业说明 —— Agent 作业 2（c0–c5 多 Agent 架构）

## 一、基本信息

- **姓名/学号**：（请填写）
- **作业选项**：选项 B —— 将 c3 的子 Agent 改造为「代码审查员」
- **运行环境**：Python 3.13.2，依赖 `python-dotenv` 和 `openrouter`
- **API 服务**：OpenRouter
- **使用模型**：`deepseek/deepseek-chat`（原仓库的 `anthropic/claude-opus-4.6` 在当前地区不可用）

## 二、文件改动清单

| 文件 | 主要改动 |
|---|---|
| `c0_json.py` | ① 模型换为 `deepseek/deepseek-chat`；② 修复 Windows 控制台 UTF-8 编码；③ `bash` 改用 `subprocess.run(encoding='utf-8')`；④ 增加 `parse()` 容错解析 JSON；⑤ `bash` 智能识别 `cat`/`type` 读文件命令，直接用 Python UTF-8 读取 |
| `c1_toolbox.py` | 同上，统一工具箱调用方式 |
| `c2_todo.py` | 同上，保留 `todo_write` 计划工具 |
| `c3_subagent.py` | **核心改动**：子 Agent 改造为代码审查员；`bash` 函数支持智能文件读取；system prompt 要求必须读取 ≥3 个文件，报告包含文件名/行号/问题/复现条件/修复建议 |
| `c4_compact.py` | 同上，保留长对话自动压缩功能 |
| `c5_guard.py` | 同上，保留危险命令黑名单和人工确认机制 |

## 三、环境准备

```powershell
# 进入项目目录
cd C:\Users\hehe\Desktop\小星球\build-your-agents

# 安装依赖（已执行）
C:/Users/hehe/python-sdk/python3.13.2/python.exe -m pip install python-dotenv openrouter
```

项目目录下已放置 `.env` 文件（内含 `OPENROUTER_API_KEY`），且 `.gitignore` 中已排除 `.env`，不会误提交。

## 四、运行测试

### c0_json.py —— JSON 结构化工具调用

```powershell
C:/Users/hehe/python-sdk/python3.13.2/python.exe c0_json.py
```

输入：

```text
用 bash 读取 demo_project/README.md
```

结果：✅ 成功调用 bash 读取文件并总结项目结构。

### c1_toolbox.py —— 专用工具箱

```powershell
C:/Users/hehe/python-sdk/python3.13.2/python.exe c1_toolbox.py
```

输入：

```text
读取 demo_project/README.md
```

结果：✅ 成功用 `read_file` 工具读取并总结。

### c2_todo.py —— 任务清单

```powershell
C:/Users/hehe/python-sdk/python3.13.2/python.exe c2_todo.py
```

输入：

```text
读取 demo_project/README.md 并总结
```

结果：✅ 先调用 `todo_write` 列计划，再执行 read_file，最后完成总结。

### c3_subagent.py —— 代码审查员（选项 B 核心）

```powershell
C:/Users/hehe/python-sdk/python3.13.2/python.exe c3_subagent.py
```

输入：

```text
请派代码审查员检查 demo_project 的代码
```

结果：✅ 主 Agent 调用 `subagent`，子 Agent 独立执行以下动作：

1. `ls demo_project/notes_app/`
2. `cat cli.py`、`cat store.py`、`cat analyze.py`（在 Windows 下被智能转为 Python UTF-8 读取）
3. 返回审查报告

**发现的 bug**（3 个）：

1. **analyze.py `summary()`**
   - 问题：当系统中没有任何带标签的笔记时，`counts` 为空列表，直接访问 `counts[0]` 会抛出 `IndexError`。
   - 复现条件：调用 `summary()` 时确保没有任何带标签的笔记。
   - 修复建议：访问 `counts[0]` 前检查 `counts` 是否为空。

2. **cli.py `delete` 命令**
   - 问题：`int(rest[0])` 未处理非数字输入，会抛出 `ValueError`。
   - 复现条件：使用非数字编号执行 `delete` 命令。
   - 修复建议：捕获 `ValueError` 并提供友好的错误信息。

3. **store.py `DATA` 路径**
   - 问题：路径拼接方式可能导致跨平台兼容性问题。
   - 复现条件：在不同操作系统上运行程序。
   - 修复建议：使用 `os.path.join` 构建路径以确保跨平台兼容性。

### c4_compact.py —— 对话压缩

```powershell
C:/Users/hehe/python-sdk/python3.13.2/python.exe c4_compact.py
```

输入：

```text
用 bash 读取 demo_project/notes_app/cli.py
```

结果：✅ 成功读取中文文件并总结；当历史消息超过 `LIMIT=12` 时会自动压缩成摘要。

### c5_guard.py —— 危险命令拦截

```powershell
C:/Users/hehe/python-sdk/python3.13.2/python.exe c5_guard.py
```

输入：

```text
用 bash 读取 demo_project/notes_app/analyze.py
```

结果：✅ 成功读取中文文件并总结；若输入包含 `rm`、`del`、`sudo` 等危险命令，会弹出 `y/n` 确认提示。

## 五、核心改动详解

### 1. 模型替换

原仓库使用 `anthropic/claude-opus-4.6`，在当前地区请求会被 OpenRouter 拒绝（`Forbidden: This model is not available in your region`）。统一替换为 `deepseek/deepseek-chat`。

### 2. 控制台编码修复

Windows 默认控制台编码为 GBK，模型输出中的 `⚠`、`🐛`、中文等字符会导致 `UnicodeEncodeError`。统一添加：

```python
import sys
sys.stdout.reconfigure(encoding='utf-8')
```

### 3. bash 工具编码与跨平台修复

`bash` 工具从 `os.popen(cmd).read()` 改为 `subprocess.run(..., encoding='utf-8', errors='replace')`，避免读取含中文文件名或文件内容时触发 `UnicodeDecodeError`。

进一步地，为了解决 Windows 上 `cat` 命令不存在、`type` 命令输出 GBK 编码导致中文文件乱码的问题，`bash` 函数增加了**智能文件读取**：

```python
def bash(cmd):
    stripped = cmd.strip()
    # 如果命令是读文件，直接用 Python 以 UTF-8 读取，避免 Windows 编码问题
    if stripped.startswith("cat ") or stripped.startswith("type "):
        path = stripped.split(" ", 1)[1].strip()
        try:
            with open(path, encoding="utf-8") as f:
                return f.read()
        except Exception as e:
            return f"错误：无法读取文件 {path}: {str(e)}"
    # 其他命令走 subprocess
    ...
```

这样无论模型发 `cat`（Linux 习惯）还是 `type`（Windows 习惯），最终都统一用 Python UTF-8 读取，保证跨平台一致。

### 4. 子 Agent 代码审查员（c3_subagent.py）

关键改动在 `subagent()` 函数：

- system prompt 身份改为「严谨的代码审查员」；
- 明确要求审查流程：先 `ls` 列文件 → 读取 ≥3 个相关文件 → 给出详细报告；
- 报告必须包含：文件名、函数、行号、问题描述、复现条件、修复建议；
- 子 Agent 只调用 `bash` 工具，不调用 `subagent` 防止递归；
- 增强解析健壮性：兼容模型偶尔返回的非标准 JSON 报告格式（如 `issues`、`findings`、`audit_report` 等）。

## 六、遇到的问题与解决

| 问题 | 现象 | 解决 |
|---|---|---|
| 模型不可用 | `Forbidden: This model is not available in your region` | 换用 `deepseek/deepseek-chat` |
| 控制台编码错误 | `UnicodeEncodeError: 'gbk' codec can't encode character` | 添加 `sys.stdout.reconfigure(encoding='utf-8')` |
| bash 输出解码错误 | `UnicodeDecodeError: 'gbk' codec can't decode byte` | `bash` 改用 `subprocess.run(encoding='utf-8', errors='replace')` |
| Windows 不支持 `cat` | 子 Agent 调用 `cat` 时报「'cat' 不是内部或外部命令」| `bash` 函数智能识别 `cat`/`type`，转用 Python `open()` 读取 |
| Windows `type` 输出 GBK 乱码 | 子 Agent 读取中文 `.py` 文件得到乱码 | 同上，统一用 Python UTF-8 读取 |
| 子 Agent 不读文件 | 直接返回空报告 | 在 system prompt 中强制要求先 `ls` 再 `cat` 至少 3 个文件 |
| 子 Agent 返回格式不稳定 | 有时丢 `done` 或 `tool` 键 | 增加格式兼容处理，允许 `issues`/`findings` 等报告对象直接作为结果 |

## 七、提交前检查

- [x] `.env` 已写入 `.gitignore`，不会提交
- [x] `c0_json.py` 可运行
- [x] `c1_toolbox.py` 可运行
- [x] `c2_todo.py` 可运行
- [x] `c3_subagent.py` 可运行，子 Agent 能发现 analyze.py 等多个 bug
- [x] `c4_compact.py` 可运行
- [x] `c5_guard.py` 可运行
- [x] `hw2/` 目录已整理，包含代码 + 说明 + `demo_project`

---
*MIT License｜郑先隽，北京师范大学心理学部教授，人本AI设计与创新*
