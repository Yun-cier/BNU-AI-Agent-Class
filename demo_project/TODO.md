# notes_app 待办

- [ ] 给 `stats` 修 bug:没有带标签的笔记时会崩(见 analyze.py)
- [ ] 加一个 `export` 命令:把所有笔记导出成一个 Markdown 文件
- [ ] `find` 目前只搜正文,让它也能按标签搜(如 `find #research`)
- [ ] 给 `delete` 加一个确认提示,防止手滑删错
- [ ] 统计里加"最近 7 天新增了几条"
